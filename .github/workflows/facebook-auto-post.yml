name: Facebook Page Auto Poster

on:
  schedule:
    # Runs at the top of every hour (UTC)
    - cron: '0 */23 * * *'
  workflow_dispatch: {}   # lets you trigger it manually from the Actions tab

permissions:
  contents: write   # needed so the workflow can commit the updated queue file

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Post next item to Facebook Page
        env:
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
        run: python scripts/post_to_facebook.py

      - name: Commit updated queue state
        run: |
          git config user.name "fb-auto-poster-bot"
          git config user.email "actions@users.noreply.github.com"
          git add posts/queue.json
          git diff --quiet --cached || git commit -m "chore: update posting queue state"
          git push
